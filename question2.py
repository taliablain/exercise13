#this prints 5, the number of characters in the string
tup = 'Hello'
print(len(tup))

#this prints 1, the comma indicates that it is an element in a tuple, and there is only one element
tup = 'Hello',
print(len(tup))